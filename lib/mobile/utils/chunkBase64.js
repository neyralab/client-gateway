import { __asyncGenerator, __await } from "tslib";
export function chunkBase64(array) {
    return __asyncGenerator(this, arguments, function* chunkBase64_1() {
        for (const element of array) {
            yield yield __await(element);
        }
    });
}
